/**
 * jest-environment node
 */

import loginvalidator from '../../../server/validators/login';

 describe('The login validator', () => {
  it('should call the next function when validation succeeds', () => {
    
  })
 })